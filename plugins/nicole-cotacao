function run(msg, matches)
  return get_nicole(msg, matches[1], "cotacao")
end

return {
    description = "Nicole Cotacao",
    usage = "!cotacao (moeda)",
    patterns = {"^!cotacao (.*)$"},
    run = run
}
